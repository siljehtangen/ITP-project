package gr2310.ourplace.ui;

public interface Controller {
  /**
   * So the controller can have a reference to the app class and set stage.
   *
   * @param app the app to set the reference to.
   */
  void setApp(App app);
}
